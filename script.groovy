def buildApp() {
 echo 'building the app..'
 echo "version ${params.VERSION}" 
}

def testApp() {
 echo 'building the app..' 
 echo "version ${params.VERSION}" 
}

def deployApp() {
 echo 'building the app..'
 echo "version ${params.VERSION}" 
}

return this
